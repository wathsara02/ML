seed: 0
device: cpu
model:
  recurrent_hidden_size: 64
  recurrent_type: none  # options: none
  critic_hidden_size: 64
algo:
  lr: 0.0003
  clip_range: 0.2
  entropy_coef: 0.01
  value_coef: 0.5
  gae_lambda: 0.95
  gamma: 0.99
  batch_size: 64
  ppo_epochs: 4
  max_grad_norm: 0.5
training:
  episodes: 20
  exp_name: small_cpu_demo
  reward_shaping: true
